# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
PyYAML==6.0.2

# LLM
groq==0.11.0

# Agent orchestration
langgraph==0.2.34
langchain-core==0.3.10

# Memory layer (local, no paid embeddings)
chromadb==0.5.5
sentence-transformers==3.1.1

# Optional pgvector backend — used when MEMORY_BACKEND=pgvector.
# Both packages are pure-python or pre-built wheels so adding them does
# NOT add a system-Postgres requirement for the default Chroma path.
psycopg[binary]==3.2.3
pgvector==0.3.6

# Observability — opt-in tracing of every graph node.
langsmith==0.1.147

# HTTP / async
httpx==0.27.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0

# Linting / typing
ruff==0.6.8
mypy==1.11.2
types-requests==2.32.0.20240914
